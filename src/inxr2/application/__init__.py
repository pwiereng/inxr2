"""
Application Layer - Layer 2

Contains use cases (business workflows) and ports (interfaces to external systems).
Depends only on the domain layer.

Dependency Rule: Depends on Domain layer only.
"""
